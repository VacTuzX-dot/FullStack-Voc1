---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contact - อินEar">
  <div class="contact-page">
    <!-- Hero Section -->
    <section class="hero-section">
      <div class="hero-bg">
        <img src="/images/bg.png" alt="Background" class="bg-image" />
        <div class="hero-overlay"></div>
      </div>
      <div class="container">
        <div class="hero-content">
          <span class="badge-label">ติดต่อเรา</span>
          <h1 class="hero-title">
            พร้อมช่วยเหลือคุณ<br/>
            <span class="highlight">ทุกคำถาม</span>
          </h1>
          <p class="hero-subtitle">
            มีคำถามเกี่ยวกับหูฟัง? ต้องการคำแนะนำ? หรืออยากแชร์ประสบการณ์? เราพร้อมรับฟังเสมอ
          </p>
        </div>
      </div>
    </section>

    <!-- Contact Content -->
    <section class="contact-section">
      <div class="container">
        <div class="contact-grid">
          <!-- Contact Info -->
          <div class="contact-info">
            <h2>ติดต่อได้หลายช่องทาง</h2>
            <p class="info-subtitle">เลือกช่องทางที่สะดวกสำหรับคุณ เราพร้อมตอบกลับโดยเร็วที่สุด</p>

            <div class="contact-cards">
              <!-- Email Card -->
              <a href="mailto:taweesaknumma@gmail.com" class="contact-card">
                <div class="card-icon">
                  <i class="bi bi-envelope-fill"></i>
                </div>
                <div class="card-info">
                  <h4>อีเมล</h4>
                  <p>taweesaknumma@gmail.com</p>
                  <span class="card-tag">ตอบกลับภายใน 24 ชม.</span>
                </div>
                <i class="bi bi-arrow-right card-arrow"></i>
              </a>

              <!-- GitHub Card -->
              <a href="https://github.com/vactuzx-dot" target="_blank" class="contact-card">
                <div class="card-icon">
                  <i class="bi bi-github"></i>
                </div>
                <div class="card-info">
                  <h4>GitHub</h4>
                  <p>github.com/vactuzx-dot</p>
                  <span class="card-tag">ดู Source Code</span>
                </div>
                <i class="bi bi-arrow-right card-arrow"></i>
              </a>

              <!-- Facebook Card -->
              <a href="https://facebook.com/vactuz" target="_blank" class="contact-card">
                <div class="card-icon">
                  <i class="bi bi-facebook"></i>
                </div>
                <div class="card-info">
                  <h4>Facebook</h4>
                  <p>facebook.com/vactuz</p>
                  <span class="card-tag">ติดตามข่าวสาร</span>
                </div>
                <i class="bi bi-arrow-right card-arrow"></i>
              </a>

              <!-- Discord Card -->
              <a href="#" class="contact-card">
                <div class="card-icon">
                  <i class="bi bi-discord"></i>
                </div>
                <div class="card-info">
                  <h4>Discord</h4>
                  <p>อินEar Community</p>
                  <span class="card-tag">พูดคุยกับชุมชน</span>
                </div>
                <i class="bi bi-arrow-right card-arrow"></i>
              </a>
            </div>
          </div>

          <!-- Contact Form -->
          <div class="contact-form-wrapper">
            <div class="form-card">
              <h3>ส่งข้อความถึงเรา</h3>
              <p>กรอกแบบฟอร์มด้านล่างและเราจะติดต่อกลับโดยเร็ว</p>

              <form class="contact-form">
                <div class="form-group">
                  <label for="name">ชื่อ</label>
                  <input type="text" id="name" placeholder="ชื่อของคุณ" required />
                </div>

                <div class="form-group">
                  <label for="email">อีเมล</label>
                  <input type="email" id="email" placeholder="you@email.com" required />
                </div>

                <div class="form-group">
                  <label for="subject">หัวข้อ</label>
                  <select id="subject">
                    <option value="">เลือกหัวข้อ</option>
                    <option value="review">ขอรีวิวหูฟัง</option>
                    <option value="consult">ขอคำปรึกษา</option>
                    <option value="collab">ติดต่อร่วมงาน</option>
                    <option value="feedback">ส่งความคิดเห็น</option>
                    <option value="other">อื่นๆ</option>
                  </select>
                </div>

                <div class="form-group">
                  <label for="message">ข้อความ</label>
                  <textarea id="message" rows="4" placeholder="รายละเอียดที่ต้องการติดต่อ..." required></textarea>
                </div>

                <button type="submit" class="submit-btn">
                  <i class="bi bi-send-fill me-2"></i>
                  ส่งข้อความ
                </button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq-section">
      <div class="container">
        <div class="faq-header">
          <h2>คำถามที่พบบ่อย</h2>
          <p>คำตอบสำหรับคำถามที่ถูกถามบ่อยๆ</p>
        </div>

        <div class="faq-grid">
          <div class="faq-card">
            <div class="faq-icon">
              <i class="bi bi-clock"></i>
            </div>
            <h4>ใช้เวลานานแค่ไหนในการตอบกลับ?</h4>
            <p>เราพยายามตอบกลับทุกข้อความภายใน 24 ชั่วโมง ในวันทำการ</p>
          </div>

          <div class="faq-card">
            <div class="faq-icon">
              <i class="bi bi-headphones"></i>
            </div>
            <h4>ขอให้รีวิวหูฟังได้ไหม?</h4>
            <p>ได้เลย! ส่งชื่อรุ่นมาได้ เราจะพิจารณาและเพิ่มในคิวรีวิว</p>
          </div>

          <div class="faq-card">
            <div class="faq-icon">
              <i class="bi bi-gift"></i>
            </div>
            <h4>รับหูฟังไปรีวิวไหม?</h4>
            <p>รับครับ ติดต่อได้ทางอีเมลพร้อมรายละเอียดสินค้า</p>
          </div>

          <div class="faq-card">
            <div class="faq-icon">
              <i class="bi bi-handshake"></i>
            </div>
            <h4>รับงาน Collaboration ไหม?</h4>
            <p>เปิดรับความร่วมมือกับแบรนด์และผู้สร้างคอนเทนต์ ติดต่อมาได้เลย</p>
          </div>
        </div>
      </div>
    </section>
  </div>
</Layout>

<style>
  .contact-page {
    background: linear-gradient(180deg, #0a0a0a 0%, #1a1a1a 50%, #0a0a0a 100%);
    min-height: 100vh;
    color: white;
    position: relative;
  }

  /* Hero Section */
  .hero-section {
    position: relative;
    padding: 120px 0 80px;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
  }

  .bg-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.3;
    filter: blur(4px);
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(180deg, rgba(10, 10, 10, 0.8) 0%, rgba(26, 26, 26, 0.9) 100%);
  }

  .hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .badge-label {
    display: inline-block;
    background: linear-gradient(135deg, #7c3aed 0%, #5b21b6 100%);
    padding: 8px 20px;
    border-radius: 50px;
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 24px;
    letter-spacing: 0.5px;
  }

  .hero-title {
    font-size: 3rem;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 24px;
  }

  .highlight {
    background: linear-gradient(135deg, #7c3aed 0%, #a78bfa 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.7);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.7;
  }

  /* Contact Section */
  .contact-section {
    padding: 60px 0 80px;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: start;
  }

  .contact-info h2 {
    font-size: 1.75rem;
    font-weight: 600;
    margin-bottom: 12px;
  }

  .info-subtitle {
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 32px;
  }

  .contact-cards {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .contact-card {
    display: flex;
    align-items: center;
    gap: 20px;
    padding: 20px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    text-decoration: none;
    color: white;
    transition: all 0.3s ease;
  }

  .contact-card:hover {
    background: rgba(124, 58, 237, 0.1);
    border-color: rgba(124, 58, 237, 0.3);
    transform: translateX(8px);
    color: white;
  }

  .contact-card .card-icon {
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 12px;
    font-size: 1.25rem;
    flex-shrink: 0;
    background: linear-gradient(135deg, #7c3aed 0%, #5b21b6 100%);
  }

  .card-info {
    flex: 1;
  }

  .card-info h4 {
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 4px;
  }

  .card-info p {
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.9rem;
    margin-bottom: 4px;
  }

  .card-tag {
    display: inline-block;
    background: rgba(124, 58, 237, 0.2);
    padding: 2px 10px;
    border-radius: 20px;
    font-size: 0.75rem;
    color: #a78bfa;
  }

  .card-arrow {
    color: rgba(255, 255, 255, 0.3);
    transition: all 0.3s ease;
  }

  .contact-card:hover .card-arrow {
    color: #a78bfa;
    transform: translateX(4px);
  }

  /* Form Card */
  .form-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 24px;
    padding: 36px;
  }

  .form-card h3 {
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 8px;
  }

  .form-card > p {
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 28px;
  }

  .form-group {
    margin-bottom: 20px;
  }

  .form-group label {
    display: block;
    font-size: 0.9rem;
    font-weight: 500;
    margin-bottom: 8px;
    color: rgba(255, 255, 255, 0.9);
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: 14px 18px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 12px;
    color: white;
    font-size: 1rem;
    font-family: inherit;
    transition: all 0.3s ease;
  }

  .form-group input::placeholder,
  .form-group textarea::placeholder {
    color: rgba(255, 255, 255, 0.4);
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: #7c3aed;
    background: rgba(255, 255, 255, 0.1);
    box-shadow: 0 0 0 3px rgba(124, 58, 237, 0.2);
  }

  .form-group select option {
    background: #1a1a1a;
    color: white;
  }

  .submit-btn {
    width: 100%;
    padding: 16px;
    background: linear-gradient(135deg, #7c3aed 0%, #5b21b6 100%);
    border: none;
    border-radius: 12px;
    color: white;
    font-size: 1rem;
    font-weight: 600;
    font-family: inherit;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .submit-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 30px rgba(124, 58, 237, 0.3);
  }

  /* FAQ Section */
  .faq-section {
    padding: 0 0 80px;
  }

  .faq-header {
    text-align: center;
    margin-bottom: 40px;
  }

  .faq-header h2 {
    font-size: 1.75rem;
    font-weight: 600;
    margin-bottom: 8px;
  }

  .faq-header p {
    color: rgba(255, 255, 255, 0.6);
  }

  .faq-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
  }

  .faq-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 24px;
    text-align: center;
    transition: all 0.3s ease;
  }

  .faq-card:hover {
    transform: translateY(-4px);
    background: rgba(124, 58, 237, 0.1);
    border-color: rgba(124, 58, 237, 0.3);
  }

  .faq-icon {
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(124, 58, 237, 0.2) 0%, rgba(91, 33, 182, 0.2) 100%);
    border-radius: 12px;
    margin: 0 auto 16px;
    font-size: 1.25rem;
    color: #a78bfa;
  }

  .faq-card h4 {
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 8px;
  }

  .faq-card p {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.6);
    line-height: 1.5;
    margin: 0;
  }

  /* Responsive */
  @media (max-width: 992px) {
    .contact-grid {
      grid-template-columns: 1fr;
    }

    .faq-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2rem;
    }

    .faq-grid {
      grid-template-columns: 1fr;
    }

    .form-card {
      padding: 24px;
    }
  }
</style>
