---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Our Services - อินEar">
  <div class="service-page">
    <!-- Hero Section -->
    <section class="hero-section">
      <div class="hero-bg">
        <img src="/images/bg.png" alt="Background" class="bg-image" />
        <div class="hero-overlay"></div>
      </div>
      <div class="container">
        <div class="hero-content">
          <span class="badge-label">บริการของเรา</span>
          <h1 class="hero-title">
            เราพร้อมช่วยคุณค้นหา<br/>
            <span class="highlight">หูฟังที่ใช่</span>
          </h1>
          <p class="hero-subtitle">
            ไม่ว่าคุณจะเป็นมือใหม่หรือนักเล่นเสียงตัวจริง เรามีบริการครบวงจรเพื่อตอบโจทย์ทุกความต้องการ
          </p>
        </div>
      </div>
    </section>

    <!-- Services Grid -->
    <section class="services-section">
      <div class="container">
        <div class="services-grid">
          <!-- Service Card 1 -->
          <div class="service-card featured">
            <div class="card-icon">
              <i class="bi bi-headphones"></i>
            </div>
            <div class="card-content">
              <h3>รีวิว IEM เชิงลึก</h3>
              <p>รีวิวหูฟังแบบละเอียดและตรงไปตรงมา พร้อมการเปรียบเทียบกับรุ่นอื่นในระดับราคาเดียวกัน</p>
              <ul class="feature-list">
                <li><i class="bi bi-check-circle-fill"></i> วิเคราะห์เสียงแบบละเอียด</li>
                <li><i class="bi bi-check-circle-fill"></i> เปรียบเทียบกับคู่แข่ง</li>
                <li><i class="bi bi-check-circle-fill"></i> ให้คะแนนตามหลักการ</li>
              </ul>
            </div>
          </div>

          <!-- Service Card 2 -->
          <div class="service-card">
            <div class="card-icon">
              <i class="bi bi-book"></i>
            </div>
            <div class="card-content">
              <h3>คู่มือเลือกซื้อ</h3>
              <p>แนะนำหูฟังที่เหมาะสมตามงบประมาณและความต้องการ</p>
              <ul class="feature-list">
                <li><i class="bi bi-check-circle-fill"></i> แบ่งตามช่วงราคา</li>
                <li><i class="bi bi-check-circle-fill"></i> แยกตามประเภทเสียง</li>
                <li><i class="bi bi-check-circle-fill"></i> อัปเดตทุกเดือน</li>
              </ul>
            </div>
          </div>

          <!-- Service Card 3 -->
          <div class="service-card">
            <div class="card-icon">
              <i class="bi bi-chat-dots"></i>
            </div>
            <div class="card-content">
              <h3>ให้คำปรึกษา</h3>
              <p>ปรึกษาเรื่องหูฟังกับทีมงานที่มีประสบการณ์โดยตรง</p>
              <ul class="feature-list">
                <li><i class="bi bi-check-circle-fill"></i> วิเคราะห์ความชอบ</li>
                <li><i class="bi bi-check-circle-fill"></i> แนะนำตรงตามสไตล์</li>
                <li><i class="bi bi-check-circle-fill"></i> ตอบกลับภายใน 24 ชม.</li>
              </ul>
            </div>
          </div>

          <!-- Service Card 4 -->
          <div class="service-card">
            <div class="card-icon">
              <i class="bi bi-bar-chart-line"></i>
            </div>
            <div class="card-content">
              <h3>เปรียบเทียบรุ่น</h3>
              <p>เครื่องมือเปรียบเทียบหูฟังหลายรุ่นพร้อมกัน</p>
              <ul class="feature-list">
                <li><i class="bi bi-check-circle-fill"></i> ตารางเปรียบเทียบ</li>
                <li><i class="bi bi-check-circle-fill"></i> กราฟความถี่</li>
                <li><i class="bi bi-check-circle-fill"></i> สรุปจุดเด่น-ด้อย</li>
              </ul>
            </div>
          </div>

          <!-- Service Card 5 -->
          <div class="service-card">
            <div class="card-icon">
              <i class="bi bi-stars"></i>
            </div>
            <div class="card-content">
              <h3>จัดอันดับ</h3>
              <p>รวมหูฟังที่ดีที่สุดในแต่ละหมวดหมู่</p>
              <ul class="feature-list">
                <li><i class="bi bi-check-circle-fill"></i> อันดับตามราคา</li>
                <li><i class="bi bi-check-circle-fill"></i> อันดับตามแนวเพลง</li>
                <li><i class="bi bi-check-circle-fill"></i> อัปเดตประจำ</li>
              </ul>
            </div>
          </div>

          <!-- Service Card 6 -->
          <div class="service-card">
            <div class="card-icon">
              <i class="bi bi-newspaper"></i>
            </div>
            <div class="card-content">
              <h3>ข่าวสารวงการ</h3>
              <p>ติดตามข่าวล่าสุดจากแบรนด์หูฟังชั้นนำ</p>
              <ul class="feature-list">
                <li><i class="bi bi-check-circle-fill"></i> รุ่นใหม่ที่จะออก</li>
                <li><i class="bi bi-check-circle-fill"></i> งานแสดงสินค้า</li>
                <li><i class="bi bi-check-circle-fill"></i> โปรโมชั่นพิเศษ</li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
      <div class="container">
        <div class="cta-card">
          <div class="cta-content">
            <h2>พร้อมค้นหาหูฟังที่ใช่?</h2>
            <p>มาเริ่มต้นกันเลย! ดูรีวิวหูฟังล่าสุดหรือติดต่อเราสำหรับคำปรึกษา</p>
          </div>
          <div class="cta-buttons">
            <a href="/" class="btn-primary-custom">
              <i class="bi bi-collection me-2"></i>ดูหูฟังทั้งหมด
            </a>
            <a href="/contact" class="btn-secondary-custom">
              <i class="bi bi-envelope me-2"></i>ติดต่อเรา
            </a>
          </div>
        </div>
      </div>
    </section>
  </div>
</Layout>

<style>
  .service-page {
    background: 
      linear-gradient(180deg, rgba(10, 10, 10, 0.85) 0%, rgba(26, 26, 26, 0.9) 50%, rgba(10, 10, 10, 0.85) 100%),
      url('/images/bg2.png');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    min-height: 100vh;
    color: white;
    position: relative;
  }

  /* Hero Section */
  .hero-section {
    position: relative;
    padding: 120px 0 80px;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
  }

  .bg-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.3;
    filter: blur(4px);
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(180deg, rgba(10, 10, 10, 0.8) 0%, rgba(26, 26, 26, 0.9) 100%);
  }

  .hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .badge-label {
    display: inline-block;
    background: linear-gradient(135deg, #7c3aed 0%, #5b21b6 100%);
    padding: 8px 20px;
    border-radius: 50px;
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 24px;
    letter-spacing: 0.5px;
  }

  .hero-title {
    font-size: 3rem;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 24px;
  }

  .highlight {
    background: linear-gradient(135deg, #7c3aed 0%, #a78bfa 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.125rem;
    color: rgba(255, 255, 255, 0.7);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.7;
  }

  /* Services Section */
  .services-section {
    padding: 60px 0 80px;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  .service-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    padding: 32px;
    transition: all 0.3s ease;
  }

  .service-card:hover {
    transform: translateY(-8px);
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(124, 58, 237, 0.5);
    box-shadow: 0 20px 40px rgba(124, 58, 237, 0.2);
  }

  .service-card.featured {
    background: linear-gradient(135deg, rgba(124, 58, 237, 0.15) 0%, rgba(91, 33, 182, 0.15) 100%);
    border-color: rgba(124, 58, 237, 0.3);
  }

  .card-icon {
    width: 60px;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #7c3aed 0%, #5b21b6 100%);
    border-radius: 16px;
    margin-bottom: 20px;
    font-size: 1.5rem;
  }

  .card-content h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 12px;
  }

  .card-content p {
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.95rem;
    line-height: 1.6;
    margin-bottom: 20px;
  }

  .feature-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .feature-list li {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 0;
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.8);
    border-top: 1px solid rgba(255, 255, 255, 0.05);
  }

  .feature-list li:first-child {
    border-top: none;
  }

  .feature-list li i {
    color: #a78bfa;
    font-size: 0.875rem;
  }

  /* CTA Section */
  .cta-section {
    padding: 0 0 80px;
  }

  .cta-card {
    background: linear-gradient(135deg, #7c3aed 0%, #5b21b6 100%);
    border-radius: 24px;
    padding: 48px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 32px;
  }

  .cta-content h2 {
    font-size: 1.75rem;
    font-weight: 600;
    margin-bottom: 8px;
  }

  .cta-content p {
    opacity: 0.9;
    margin: 0;
  }

  .cta-buttons {
    display: flex;
    gap: 16px;
    flex-shrink: 0;
  }

  .btn-primary-custom {
    display: inline-flex;
    align-items: center;
    padding: 14px 28px;
    background: white;
    color: #7c3aed;
    text-decoration: none;
    border-radius: 12px;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .btn-primary-custom:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
    color: #5b21b6;
  }

  .btn-secondary-custom {
    display: inline-flex;
    align-items: center;
    padding: 14px 28px;
    background: rgba(255, 255, 255, 0.15);
    color: white;
    text-decoration: none;
    border-radius: 12px;
    font-weight: 600;
    border: 1px solid rgba(255, 255, 255, 0.3);
    transition: all 0.3s ease;
  }

  .btn-secondary-custom:hover {
    background: rgba(255, 255, 255, 0.25);
    transform: translateY(-2px);
    color: white;
  }

  /* Responsive */
  @media (max-width: 992px) {
    .services-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2rem;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .cta-card {
      flex-direction: column;
      text-align: center;
      padding: 32px;
    }

    .cta-buttons {
      flex-direction: column;
      width: 100%;
    }

    .btn-primary-custom,
    .btn-secondary-custom {
      justify-content: center;
    }
  }
</style>
